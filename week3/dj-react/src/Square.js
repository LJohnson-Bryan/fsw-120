import React from 'react';

const Square = (props) => {
    return ( <div style={{backgroundColor: props.color, display: "flex", alignItems: "center", justifyContent: "center"}}>DJ Square</div> );
}

export default Square;
